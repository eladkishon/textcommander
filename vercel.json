{
    "builds": [
      {
        "src": "app/package.json",
        "use": "@vercel/next"
      }
    ],
    "buildCommand": "pnpm install && cd app && pnpm install && pnpm run build",
    "outputDirectory": "app/.next"
  }
  